- **feat(providers):** publish the Gemini 3.7 Flash High and Medium tiers on the Antigravity CLI (`agy`) provider, mirroring the upstream ids already shipped for `antigravity`. Both providers front the identical Antigravity backend, executor, translator and OAuth client, so the tiers were already dispatchable — only the `agy` catalog surface was missing them, leaving the models callable through one provider and invisible through the other. Adds a parity test so the two Gemini 3.7 catalogs cannot drift apart again. ([#10665](https://github.com/diegosouzapw/OmniRoute/pull/10665)) — thanks @yawar-aquil
